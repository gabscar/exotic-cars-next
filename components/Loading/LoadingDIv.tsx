import { Loading, LoadingContainer } from "./LoadingComponent"

export const LoadingDiv: React.FC = ()=>{

    return(
        <LoadingContainer>
            <Loading/>
        </LoadingContainer>
    )
}